# Authority

Authority is computed from explicit factors (source type, origin, verification,
evidence strength, recency, scope match, supersession). A discrete **trust
band** (`canonical` / `high` / `medium` / `low` / `untrusted`) is assigned
from those factors. `source_reliability` is deferred. See
[authority model](../architecture/authority-model.md).
