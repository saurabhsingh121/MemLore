# REST API

Bootstrap surface:

| Method | Path | Purpose |
|--------|------|---------|
| `GET` | `/health` | Liveness payload (`status`, `service`, `version`) |

Additional REST resources for lore entries, verification, and search will be
specified with the first product feature and covered by contract tests.
