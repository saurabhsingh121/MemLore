---
alwaysApply: true
---

<!-- SPECKIT START -->
# MemLore project context for Spec Kit

- Language: Python 3.12
- Package: `src/memlore/` (domain / application / infrastructure / adapters / bootstrap)
- Tests: `tests/{unit,integration,contract,e2e}` with pytest; TDD mandatory for behavior
- Governance DB: PostgreSQL; Knowledge: Graphiti + Neo4j; async sync via outbox
- Adapters: FastAPI REST, MCP (`memlore.*`), CLI (`memlore`)
- Tooling: uv, ruff, mypy --strict, Docker Compose
- Constitution: `.specify/memory/constitution.md` (v1.0.0)
- Brand: MemLore (not Tavryn) per ADR 0004

## Active feature plan

Read `specs/001-scoped-lore-entry/plan.md` for the current feature
(scoped human-authored lore entry: store / retrieve / verify / list / audit).
Also see `research.md`, `data-model.md`, `contracts/rest-lore-entries.md`,
and `quickstart.md` in that directory.
<!-- SPECKIT END -->
